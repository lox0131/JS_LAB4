import { useEffect, useState } from "react"

const PersonalSide = ( { methods } ) => {
    
    const [saved, setSaved] = useState([]);
    
    useEffect(() => {
        
    }, [])

    return (
        <div>
            
        </div>
    )
}

export default PersonalSide
